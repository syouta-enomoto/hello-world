# hello-world

### 使うもの
- Node.js

### install
- anyenvをinstall
  - (ry
- nodenvをinstall
  - (ry
- Node.jsをinstall
  - (ry

### 実行
- サーバーの起動
```
npm install
npm start
```
- ブラウザからhttp://localhost:3000/ へアクセス
  - AWSへdeployしてグローバルIPでアクセスできるようにする
  